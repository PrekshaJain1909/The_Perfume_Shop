# Perfume Shop server environment variables
# Replace placeholder values with your real credentials before starting the server.

# Server port
PORT=5000

# MongoDB connection URI (use your MongoDB connection string)
# Example local: mongodb://127.0.0.1:27017/perfumeShop
# Example Atlas: mongodb+srv://<user>:<pass>@cluster0.xyz.mongodb.net/perfumeShop?retryWrites=true&w=majority
MONGO_URI=mongodb+srv://prekjainsha190994_db_user:PerfumeShop@cluster0.1kapeia.mongodb.net/
# Email (SMTP) settings — required if you want the server to send receipts
# If you don't want to send email, you can leave these empty; the server will return the PDF as base64 instead.
MAIL_HOST=
MAIL_PORT=
MAIL_USER=
MAIL_PASS=
MAIL_FROM=

# Optional: change the FROM address shown when sending receipts
# MAIL_FROM=Perfume Shop <no-reply@example.com>

# Any other secrets you may need (JWT, etc.)
# JWT_SECRET=change_me
